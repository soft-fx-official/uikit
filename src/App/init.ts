import { IInitApp, IInitStateR, IInitStorageR } from 'common/inits'
import { IBus } from 'common/tools'
import { i18n } from 'i18next'

async function init({ storage, i18next, state, bus, api }: IInitApp) {}

export default init
